---
title: Resource API
description: Font, image, and sound resource APIs
---

# Resource API

## Font

### `dmn.font.load()`

Opens the system file picker, imports a font, and returns its app-managed path.

```typescript
const result = await dmn.font.load();
// {
//   success: boolean,
//   error?: string,
//   fontName?: string,
//   fontPath?: string,
//   weightRanges?: Array<{ min: number, max: number }>
// }
```

`fontName` is the family name read from the font file's own metadata (the file name is used only when the font has no readable family record), so several files of one family import as one family. If the file cannot be parsed as a font, `success` is `false` and nothing is copied.

## Image

### `dmn.image.load()`

Opens the system file picker, imports an image, and returns its app-managed path.

```typescript
const result = await dmn.image.load();
// { success: boolean, error?: string, imagePath?: string }
```

## Sound

See the dedicated [Sound API reference](/docs/api-reference/sound) for sound library visibility, editing, and compatibility APIs.

Paths returned by resource APIs must be treated as opaque app-managed identifiers.
