**한국어** | [English](mac_guide_en.md) | [中文](mac_guide_zh-cn.md)

# macOS 설치 및 권한 설정 가이드

## 1. 권한 설정

DM NOTE가 키보드 및 마우스 입력을 캡처하려면 두 가지 권한이 필요합니다:

### 필수 권한

1. **손쉬운 사용(접근성)** — Accessibility
2. **입력 모니터링** — Input Monitoring

### 설정 방법

앱을 처음 실행하면 **손쉬운 사용(접근성)** 권한 요청 팝업이 자동으로 표시됩니다.

**입력 모니터링** 권한은 팝업이 표시되지 않을 수 있으므로, 수동으로 설정해야 합니다:

1. **시스템 설정**(System Settings) 앱을 엽니다
2. **개인정보 보호 및 보안**(Privacy & Security)로 이동합니다
3. **손쉬운 사용**(Accessibility)을 클릭합니다
   - `+` 아이콘을 클릭하여 DM NOTE.app을 추가합니다.
4. **입력 모니터링**(Input Monitoring)을 클릭합니다
   - `+` 아이콘을 클릭하여 DM NOTE.app을 추가합니다.

### ⚠️ 중요: 앱 재시작 필수

권한을 부여한 후에는 **DM NOTE를 완전히 종료하고 다시 실행**해야 권한이 적용됩니다.

---

## 2. 1.6.1 이하 버전 관련 문제

> [!WARNING]
> **1.6.1 이하 버전은 Apple Developer ID 서명과 공증(Notarization)이 적용되지 않았습니다.**
>
> 이 문제는 macOS Gatekeeper 보안 정책 때문에 발생하며, 새 버전 설치 시 macOS가 이전 버전과 다른 앱으로 인식해 권한이 초기화됩니다. 2.0.0 이상 버전에서는 해결되었으므로 특별한 이유가 없다면 [최신 버전](https://github.com/DmNote-App/DmNote/releases/latest) 사용을 권장합니다.

### 2-1. 설치 후 "손상되어 열 수 없습니다" 오류

앱을 처음 실행할 때 다음과 같은 메시지가 표시될 수 있습니다:

> **"DM NOTE"이(가) 손상되었기 때문에 열 수 없습니다.**

**터미널**을 열고 다음 명령어를 실행하세요:

```bash
sudo xattr -cr /Applications/DM\ NOTE.app
```

- 비밀번호 입력을 요구하면 macOS 로그인 비밀번호를 입력하세요
- 명령어 실행 후 앱을 다시 실행하세요

### 2-2. 업데이트 후 권한이 작동하지 않음

- 기존에 권한을 허용했는데도 키 입력이 캡처되지 않음
- 설정에서 DM NOTE 권한이 이미 활성화되어 있지만 작동하지 않음

이때는 **기존 권한을 제거하고 다시 추가**해야 합니다:

1. **시스템 설정** → **개인정보 보호 및 보안**으로 이동
2. **손쉬운 사용**(Accessibility)을 엽니다
   - **DM NOTE**를 선택하고 `-` 아이콘을 클릭하여 제거합니다
3. **입력 모니터링**(Input Monitoring)을 엽니다
   - **DM NOTE**를 선택하고 `-` 아이콘을 클릭하여 제거합니다
4. **DM NOTE 앱을 완전히 종료**합니다
5. DM NOTE를 다시 실행하고 [1. 권한 설정](#1-권한-설정) 섹션의 방법으로 권한을 다시 추가합니다
6. 앱을 재시작합니다

---

## 문제 해결

### 키 입력이 캡처되지 않음

1. 두 권한(손쉬운 사용 + 입력 모니터링)이 모두 활성화되어 있는지 확인
2. 앱을 완전히 종료하고 재시작
3. 1.6.1 이하에서 업데이트했다면 위의 [2-2. 업데이트 후 권한이 작동하지 않음](#2-2-업데이트-후-권한이-작동하지-않음) 참고

### 앱이 열리지 않음

1.6.1 이하 버전이라면 위의 [2-1. 설치 후 "손상되어 열 수 없습니다" 오류](#2-1-설치-후-손상되어-열-수-없습니다-오류) 참고
